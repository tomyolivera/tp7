@{
    ViewData["Title"] = "Habitacion 3";
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-6">
        <img width="100%" src="~/img/black-mirror.jpg" draggable="false" class="blur rounded" />
    </div>

    <div class="col-6">
        <div id="pista" class="d-none mb-5">
            <h3>Pista</h3>
            <p>La serie está basada en contar distopías posibles y cercanas según las amenazas existen a nuestro alrededor: la obsesión por las redes sociales, la inteligencia artificial, la acumulación de datos sobre nuestra vida, entre otras.</p>
        </div>

        <div>
            <h4>Ingrese el título de la serie</h4>

            <form method="POST" action='@Url.Action("Habitacion3", "Escape")'>
                <div class="form-group">
                    <input name="clave" autocomplete="off">
                </div>

                <div class="d-flex">
                    <button class="btn btn-success">Comprobar</button>
                    <button onclick="$('#pista').removeClass('d-none')" type="button" class="btn btn-clave btn-danger mx-2">Pista</button>
                </div>
            </form>
        </div>
    </div>
</div>